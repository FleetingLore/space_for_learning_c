# 1

**题目**

有一函数:

$y = $

$x\ \ \ \ (x < 1 )$  

$2x - 1\ \ \ \ (1 \le x < 10)$

$3x - 11\ \ \ \ (10 \le x)$

写一个函数，输入 `x`，输出 `y`。

**题解**

构建分支结构即可。

[解决方案](./1.c)

---

# 2

**题目**

个人所得税，应纳税款的计算公式如下：

（懒得抄，自己看吧）

用 `if` 和 `switch` 分别实现。

**题解**

呵呵

[解决方案](./2.c)

---

# 3

**题目**

电文加密算法，`A` 变 `G`，`a` 变 `g`，`B` 变 `H`，`b` 变 `h`，以此类推，并且 `U` 变 `A`，`V` 变 `B`。

**题解**

有点像上次的那个题。

分成四部分映射。

[解决方案](./3.c)
